import {Frame} from '../Marketing';import Link from 'next/link';
export default function Solutions(){return <Frame><main className="marketing-section"><span className="eyebrow">SOLUTIONS</span><h1 className="editorial-title">One workspace, tailored to your role.</h1><p className="intro">A developer keeps the canonical project record while approved partners work in their assigned scope.</p><div className="persona-grid" style={{marginTop:35}}>{[['Developers','View availability, sale progression, approvals, delivery and open issues across projects.'],['Project agencies','Work on assigned projects without moving the developer’s records into a second copy.'],['Buyers','See your own property, next task, documents, payment plan and home care.'],['Contractors','See only the current assigned jobs, arrange access and provide proof of work.']].map(([t,b])=><div className="surface surface-pad" key={t}><h2>{t}</h2><p className="muted" style={{marginTop:10}}>{b}</p></div>)}</div><Link href="/contact" className="btn primary" style={{marginTop:32}}>Book a demo →</Link></main></Frame>}
