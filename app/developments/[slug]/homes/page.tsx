import {notFound} from 'next/navigation';import Link from 'next/link';import {development} from '../../data';import {ProjectLayout} from '../../ProjectLayout';import HomesFilter from './HomesFilter';
export const dynamic='force-dynamic';
export default async function Homes({params}:{params:Promise<{slug:string}>}){const {slug}=await params,project=await development(slug);if(!project)notFound();return <ProjectLayout project={project}><main className="marketing-section"><span className="eyebrow">{project.name.toUpperCase()} / INVENTORY</span><h1 className="editorial-title">Find your home.</h1><p className="intro">Filter current homes and explore their details. Availability is checked again before any request.</p><HomesFilter homes={project.homes} slug={slug}/></main></ProjectLayout>}
